# Build context = ./web. Keep it lean and reproducible.
# Runtime-collected static lives in the static_volume, not the image.
staticfiles/
# Python/editor cruft
**/__pycache__
**/*.py[cod]
*.log
.pytest_cache
.mypy_cache
core
core.*
